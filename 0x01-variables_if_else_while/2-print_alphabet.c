#include <stdio.h>

/**
 * main - Entry point
 * Descriptio:
 * Returns: Always 0 (Success)
 */

int main(void)
{
	int alpha;
	for (alpha = 'a'; alpha <= 'z'; alpha++)
	{
		putchar(alpa);
	}
	return (0);
}
